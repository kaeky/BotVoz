import { EventOptions } from '../util/Constants';
export declare class Event {
    client: any;
    name: string;
    once: boolean;
    ws: boolean;
    options: any;
    _path: string;
    constructor(client: any, options: EventOptions);
    run(client: any, ...args: any[]): void;
    reload(): Promise<true | void>;
}
