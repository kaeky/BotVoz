import { Response } from '../structures/Response';
import { PayloadRequest } from '../util/constants';
import { HPromise } from '../structures/HPromise';
export declare class Hyttpo {
    constructor();
    request(data: PayloadRequest): HPromise<Response>;
    private rawRequest;
    get(url: string, data?: PayloadRequest): void;
    post(url: string, data?: PayloadRequest): void;
    patch(url: string, data?: PayloadRequest): void;
    put(url: string, data?: PayloadRequest): void;
    trace(url: string, data?: PayloadRequest): void;
    head(url: string, data?: PayloadRequest): void;
    options(url: string, data?: PayloadRequest): void;
    connect(url: string, data?: PayloadRequest): void;
    delete(url: string, data?: PayloadRequest): void;
    search(url: string, data?: PayloadRequest): void;
}
